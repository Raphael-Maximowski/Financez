
export interface calendarMaskInterface {
    modelValue: string;
}

export interface moneyMaskSettingsInterface {
  decimal: string;
  thousands: string;
  prefix: string;
  precision: number;
  masked: boolean;
}